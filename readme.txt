As for this version, it actually can track all the method in a specfic package rather than only a particular test case.

However, as for the data structure, it cannot cope with the most complicated section in the part three.